用户原话"活动页的分享按钮去掉"有实质歧义(需求:要移除分享按钮 vs bug:分享按钮异常所以要去掉),技能第一轮必须只做消歧。合格(PASS)须同时满足:

1. 回复里恰好一道选择题,且是消歧题——询问这句话是哪种读法/意图;
2. 选项列出至少两种实质不同的读法,末项允许用户自由回复(如"其他(直接回复你的说法)"或等价说法);
3. 没有出现维度选择题(如问给谁看/什么形式/什么平台);
4. 没有出现任何"优化后的提示词"成品(尤其是代码块包着的成品);
5. 没有替用户假定某一种读法直接往下走。

任何一条不满足即 FAIL。
