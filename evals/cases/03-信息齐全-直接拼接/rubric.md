用户原话无歧义,且受众(给开发)与形式(一句话 bug 描述)都已给出——没有实质未述维度,技能应直接进第三步纯拼接。合格(PASS)须同时满足:

1. 不问任何问题(没有消歧题、没有维度题);
2. 直接给出拼接成品,且成品放在代码块里;
3. 成品只包含用户给过的信息点:Safari、登录按钮点击无反应、期望进入首页、给开发提 bug、一句话描述(允许语序调整与词形理顺,如"点了没反应"→"点击后没有反应");
4. 零编造:不得出现浏览器/系统版本号、复现步骤、严重级别、角色设定("你是一位资深测试工程师"之类)、角色/背景/约束式模版分段,或任何用户没说过的细节;
5. 代码块外最多一句话说明(如"已拼接完成,可直接发送"),不解释流程、不列假设、不给备选版本。

任何一条不满足即 FAIL。
