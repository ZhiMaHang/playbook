用户点名四段式,"目标"已由原话给出(把这个季度的客户投诉归类),背景/输出要求/边界约束均未述。合格(PASS)须同时满足:

1. 走的是目标/背景/输出要求/边界约束的四段式流程,不是五句话骨架(不出现"我要做/给谁用/解决什么问题"等槽位),也没有消歧题;
2. 不重复问"目标"是什么(原话已给出);
3. 就背景、输出要求、边界约束三个空槽提问,在同一条回复里一次给出;
4. 每道题末项允许用户自由回复(如"其他(直接回复你的说法)"或等价说法);
5. 选项不夹带用户没说过的事实细节(可以给体裁/篇幅这类类别级取值,但不能编造数据来源、业务范围、具体约束内容等);
6. 没有出现任何四段式成品。

任何一条不满足即 FAIL。
